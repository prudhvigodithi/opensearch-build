plugins {
    id 'java'
    id 'maven-publish'
    id 'java-gradle-plugin'
}

group 'org.opensearch.gradle'
version '1.0'

repositories {
    mavenLocal()
}

gradlePlugin {
    plugins {
        update4j {
            id = 'zipPlugin'
            implementationClass = "org.opensearch.gradle.zipplugin.ZipPublish"
        }
    }
}

publishing {
    repositories {
        mavenLocal()
    }
    /*publications {
        mavenJava(MavenPublication) {
            artifact sourcesJar
            artifact javadocJar
        }
    }*/
}

/*task sourcesJar(type: Jar) {
    classifier = 'sources'
    from sourceSets.main.allSource
    }

task javadocJar(type: Jar) {
    classifier = 'javadoc'
    from javadoc
    }

artifacts {
    archives sourcesJar
    archives javadocJar
    }*/


